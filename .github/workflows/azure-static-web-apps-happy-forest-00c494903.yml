name: Azure Static Web Apps CI/CD

on:
  push:
    branches: [master]
    paths:
      - 'packages/stories/**'
      - 'packages/ora-components/src/**'
      - 'packages/ora-components/package.json'
      - '.github/workflows/azure-static-web-apps-happy-forest-00c494903.yml'

concurrency:
  group: swa-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  id-token: write

jobs:
  build_and_deploy_job:
    runs-on: ubuntu-latest
    name: Build and Deploy Job
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: true
          lfs: false
          fetch-depth: 2

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: package-lock.json

      - name: Install dependencies
        run: npm ci --no-audit --no-fund

      - name: Build ora-components
        run: npx turbo run build --filter=@tdq/ora-components

      - name: Build Storybook
        run: npm run build-storybook -w aura-stories

      - name: Verify Storybook output
        run: ls packages/stories/storybook-static/index.html

      - name: Build And Deploy
        if: steps.detect.outputs.should_deploy == 'true'
        id: builddeploy
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN_HAPPY_FOREST_00C494903 }}
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          action: "upload"
          skip_app_build: true
          app_location: "packages/stories/storybook-static"
          api_location: ""
