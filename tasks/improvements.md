# Improvements tasks
1. **GridBuilder** addMoneyColumn should accept Money object instead of Number
2. **GridBuilder** "editable" feature is not working
3. Implement component which accepts observable and allows to define how render observable value
4. Export `registerDestroy`