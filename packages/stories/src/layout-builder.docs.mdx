import { Meta, Canvas } from '@storybook/addon-docs/blocks';
import * as Stories from './layout-builder.stories';

<Meta of={Stories} />

# Layout

The `LayoutBuilder` provides a flexible, reactive API for constructing horizontal and vertical layouts with slot-based content placement. Each slot can hold a single child component and supports configurable sizing, visibility, and alignment — all driven by RxJS observables.

Layouts are the foundational building block for arranging panels, sidebars, headers, and content regions in financial dashboards and data-heavy interfaces.

---

## When to use

Use `LayoutBuilder` when you need to arrange multiple components in a horizontal or vertical stack:

- **Page shells** — sidebar, main content, and footer regions arranged in a vertical column
- **Split panels** — side-by-side panels with proportional widths using `SlotSize` (e.g. 1/4 sidebar + 3/4 content)
- **Dashboard grids** — row-based or column-based arrangements of cards, charts, and data tables
- **Conditional layouts** — slots that appear or disappear based on application state via `withVisible`

Use CSS Grid or Flexbox directly for simple one-off arrangements. Use `LayoutBuilder` when slots need reactive visibility, proportional sizing, or integration with other component builders.

---

## Usage

```typescript
import { LayoutBuilder, LayoutGap } from '@tdq/ora-components';
import { of } from 'rxjs';

// Vertical layout with three slots
const layout = new LayoutBuilder()
    .asVertical()
    .withGap(LayoutGap.LARGE);

layout.addSlot().withContent(headerComponent);
layout.addSlot().withContent(contentComponent);
layout.addSlot().withContent(footerComponent);

const element = layout.build();
document.body.appendChild(element);
```

---

## Builder API

All builder methods return `this` for chaining. Pass RxJS observables into reactive setters — the component handles subscription lifecycle and automatically cleans up when detached from the DOM.

### LayoutBuilder

- **`addSlot`** — `() => SlotBuilder`
  Creates and returns a new `SlotBuilder` for configuring a child slot. Slots are rendered in the order they are added.

- **`asVertical`** — `() => this`
  Sets the layout direction to vertical (column). Children stack top-to-bottom.

- **`asHorizontal`** — `() => this`
  Sets the layout direction to horizontal (row). Children flow left-to-right. Default if not set.

- **`withGap`** — `(gap: LayoutGap) => this`
  Sets the gap between adjacent slots. Accepts a `LayoutGap` enum value. Default is `LayoutGap.MEDIUM`.

- **`withClass`** — `(className: Observable<string>) => this`
  Appends additional CSS class names to the root layout element. Useful for theme overrides or spacing utilities.

- **`withAlignment`** — `(alignment: Observable<Alignment>) => this`
  Sets the default alignment for all child slots that do not have their own alignment override. Only applies to horizontal layouts.

- **`build`** — `() => HTMLElement`
  Constructs and returns the layout `<div>` element with all configured slots. Call once per builder.

### SlotBuilder

Returned by `addSlot()`. Each slot wraps a single child component.

- **`withContent`** — `(content: ComponentBuilder) => SlotBuilder`
  Sets the child component displayed inside this slot. Accepts any `ComponentBuilder` instance.

- **`withSize`** — `(size: SlotSize) => SlotBuilder`
  Sets the slot width as a fraction of available space using the `SlotSize` enum. For horizontal layouts, defaults to equal distribution (`flex-1`). For vertical layouts, defaults to content-fit.

- **`withVisible`** — `(visible: Observable<boolean>) => SlotBuilder`
  Controls slot visibility. When the observable emits `false`, the slot is hidden (`display: none`).

- **`withAlignment`** — `(alignment: Observable<Alignment>) => SlotBuilder`
  Overrides the layout-level alignment for this specific slot.

- **`withName`** — `(name: string) => SlotBuilder`
  Sets the slot's `data-slot` attribute value, useful for styling hooks, testing selectors, and debugging layout structure in devtools. When not set, the slot defaults to its zero-based index as a string (`data-slot="0"`, `data-slot="1"`, ...). In dev mode, `LayoutBuilder` warns via `console.warn` if two slots in the same layout resolve to the same effective `data-slot` value (whether both explicit or one explicit colliding with an index) — `data-slot` values should be unique within a layout.

---

## Enums

### LayoutGap

Defines the gap between adjacent slots.

| Value | CSS Value | Description |
|-------|-----------|-------------|
| `LayoutGap.NONE` | `gap-0` | No gap between slots. |
| `LayoutGap.SMALL` | `gap-1` | 4px gap. |
| `LayoutGap.MEDIUM` | `gap-2` | 8px gap (default). |
| `LayoutGap.LARGE` | `gap-4` | 16px gap. |
| `LayoutGap.EXTRA_LARGE` | `gap-8` | 32px gap. |

### SlotSize

Defines the proportional width of a slot relative to available space.

| Value | CSS Class | Fraction |
|-------|-----------|----------|
| `SlotSize.QUARTER` | `basis-1/4` | 1/4 of available space |
| `SlotSize.THIRD` | `basis-1/3` | 1/3 of available space |
| `SlotSize.HALF` | `basis-1/2` | 1/2 of available space |
| `SlotSize.TWO_THIRDS` | `basis-2/3` | 2/3 of available space |
| `SlotSize.THREE_QUARTERS` | `basis-3/4` | 3/4 of available space |
| `SlotSize.FULL` | `basis-full` | Full available width |
| `SlotSize.FIT` | `flex-none` | Sized to content |
| `SlotSize.GROW` | `flex-1` (+ `min-h-0`/`min-w-0`) | Fills the remaining space along the layout's main axis and may shrink below its content's intrinsic size to do so |

For horizontal layouts, slots without an explicit size use `flex-1` (equal distribution). For vertical layouts, slots default to content height.

#### `SlotSize.GROW` — needs a definite-height ancestor

`GROW` is the odd one out: it doesn't just set a size class on the slot — as soon as **any** slot in a layout is `GROW`, `LayoutBuilder` also adds `h-full min-h-0` (vertical) or `min-w-0` (horizontal) to the **layout container itself**, so the GROW slot has something concrete to grow against and CAN shrink below its content's natural size instead of overflowing.

`h-full`, however, resolves to nothing if the layout's own ancestor chain doesn't have a **definite height** — a `<div>` with no set height, or one sized only by its content, gives `h-full` `height: 100%` of an undefined value. In that case the GROW slot has no bounded space to fill and collapses to its content size, exactly like `FIT`. Always give the layout (or an ancestor) an explicit height — e.g. `h-screen`, `h-[500px]`, or a flex/grid parent that itself resolves to a definite height — before relying on `GROW` to fill it. See `VerticalGrowSlot` below, which wraps the layout in a `h-[500px]` container specifically for this reason.

<Canvas of={Stories.VerticalGrowSlot} />

### Alignment

Defines the horizontal alignment of content within a slot.

| Value | Description |
|-------|-------------|
| `Alignment.LEFT` | Content aligned to the left (default). |
| `Alignment.RIGHT` | Content aligned to the right. |
| `Alignment.CENTER` | Content centered within the slot. |

---

## Horizontal Layout

The `Horizontal` story renders three equally sized slots side by side with a medium gap, each containing a coloured placeholder.

<Canvas of={Stories.Horizontal} />

---

## Vertical Layout

The `Vertical` story stacks three slots (Header, Content, Footer) top-to-bottom with a large gap, demonstrating a typical page shell structure.

<Canvas of={Stories.Vertical} />

---

## Mixed Sizes

The `MixedSizes` story shows a horizontal layout where each slot has an explicit `SlotSize` — a sidebar (1/4), main content (1/2), and a right panel (1/4) — separated by an extra-large gap.

<Canvas of={Stories.MixedSizes} />

---

## Conditional Visibility

The `ConditionalVisibility` story demonstrates reactive slot visibility controlled by a `BehaviorSubject<boolean>`. A toggle button shows and hides the middle slot, while the other two slots reflow to fill the available space.

<Canvas of={Stories.ConditionalVisibility} />

---

## Slot Alignment

The `AlignmentStory` shows three vertical slots with individually configured alignment — left, center, and right — each demonstrated with a coloured placeholder.

<Canvas of={Stories.AlignmentStory} />

---

## Layout Alignment

The `LayoutAlignmentStory` demonstrates setting alignment at the layout level using `withAlignment(of(Alignment.CENTER))`. All child slots inherit the centered alignment without needing individual configuration.

<Canvas of={Stories.LayoutAlignmentStory} />

---

## Styling

All visual properties are driven by MD3 design tokens. Override them on `:root` or a scoped parent to restyle this component without touching source.

| CSS variable | Role |
|---|---|
| `--md-sys-spacing-*` | Gap between slots — `LayoutGap` enum maps to spacing tokens |
| `--md-sys-color-surface` | Layout container background (when glass effect is applied) |

See the [Theming guide](../?path=/docs/system-theming--docs) for token reference and custom theme examples.

### Additional notes

- **Full width**: The layout container always applies `w-full` for full-width behaviour.
- **Direction**: `asVertical()` applies `flex-col`; `asHorizontal()` (default) applies `flex-row`.
- **Gaps** are implemented with Tailwind `gap-*` utility classes based on the `LayoutGap` enum.
- **Slot sizing**: Horizontal slots without an explicit `SlotSize` receive `flex-1` to distribute space equally. Vertical slots default to content height.
- **`data-slot` attribute**: every rendered slot wrapper carries a `data-slot` attribute — `withName('scroll-area')` sets it explicitly, otherwise it defaults to the slot's zero-based index. Useful for styling hooks and test selectors. `LayoutBuilder` warns (dev mode only) if two slots in the same layout resolve to the same `data-slot` value.
- **Visibility**: Toggling slot visibility uses `display: none` via inline styles, driven by the `withVisible` observable. The component handles cleanup on destroy.
- **Alignment**: Layout-level alignment applies only to horizontal layouts. Slot-level alignment always applies. When both are set, the slot-level alignment takes precedence.

---

## Related components

- [Panel](../?path=/docs/components-panel--docs) — use panels inside layout slots for grouped content regions
- [Grid](../?path=/docs/components-grid--docs) — use grids inside layout slots for tabular data displays
